<h2><%= branches.count %> <%= title %></h2>
<ul>
	<% branches.each do |branch| %>
		<li><%= branch.repo_name %> <%= branch.name %> days since last commit: <%= branch.days_since_last_commit %></li>
	<% end %>
</ul>
